package org.example;

/**
 * Interfaz que define como se comporta cualquier cosa que pueda ser
 * invitada a una reunión (como empleados individuales, departamentos completos
 * o invitados externos).
 */
public interface Invitable {
    void invitar();
}
